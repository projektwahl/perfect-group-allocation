receivers:
  otlp:
    protocols:
      grpc:
      http:

processors:
  batch:

exporters:
  otlphttp/jaeger:
    endpoint: http://jaeger:4318
  otlp/opensearch:
    endpoint: opensearch-data-prepper:21890
    tls:
      insecure: true

service:
  pipelines:
    traces:
      receivers: [otlp]
      processors: [batch]
      exporters:
        - otlphttp/jaeger
        - otlp/opensearch
    metrics:
      receivers: [otlp]
      processors: [batch]
      exporters:
        - otlphttp/jaeger
        - otlp/opensearch
    logs:
      receivers: [otlp]
      processors: [batch]
      exporters:
        - otlphttp/jaeger
        - otlp/opensearch
