apiVersion: v1
kind: Service
metadata:
  name: {{ include "perfect-group-allocation.fullname" . }}
  labels:
    {{- include "perfect-group-allocation.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "perfect-group-allocation.selectorLabels" . | nindent 4 }}
