apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "perfect-group-allocation.fullname" . }}-test-connection"
  labels:
    {{- include "perfect-group-allocation.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "perfect-group-allocation.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
