apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "perfect-group-allocation.fullname" . }}-test-connection"
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "perfect-group-allocation.fullname" . }}']
  restartPolicy: Never
