apiVersion: v1
kind: Service
metadata:
  name: {{ include "perfect-group-allocation-postgres.fullname" . }}
  labels:
    {{- include "perfect-group-allocation-postgres.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "perfect-group-allocation-postgres.selectorLabels" . | nindent 4 }}
