apiVersion: v1
kind: Service
metadata:
  name: {{ include "perfect-group-allocation-keycloak.fullname" . }}
  labels:
    {{- include "perfect-group-allocation-keycloak.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "perfect-group-allocation-keycloak.selectorLabels" . | nindent 4 }}
